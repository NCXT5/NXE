#pragma once

namespace nxe::lexer {
/// This is a class used for storing handlers for matching to tokens.
/// It can mainly be used for lexers, but other things will work too if
/// your parameters match.
class ScHnd {

};
}
